# Copy to .env and fill in your forum's actual URL. .env is gitignored so the
# real forum host never lands in source control; docker compose (and `make`,
# which just shells out to `docker compose`) loads .env automatically from
# this directory.
SCRAPE_BOARD_URL=https://forum.example.com/forum/9/forum-title
